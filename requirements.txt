# Core framework
Django>=4.2,<5.0

# Production WSGI server (Railway uses this to run the app)
gunicorn>=21.2

# Static file serving without a separate web server
whitenoise>=6.6

# PostgreSQL database driver (Railway provides PostgreSQL)
dj-database-url>=2.1
psycopg2-binary>=2.9

# Export formats
openpyxl>=3.1
reportlab>=4.0
